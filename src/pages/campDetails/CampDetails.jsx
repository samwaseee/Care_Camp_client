import { useLoaderData } from "react-router-dom";

const CampDetails = () => {

    const camp = useLoaderData();

    return (
        <div className='py-20'>
            
        </div>
    );
};

export default CampDetails;